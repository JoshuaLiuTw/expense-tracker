<div class="card card-body">
  <h1 class="text-center mt-3">必填欄位* 請修改您的支出</h1>
  <form action="/records/{{ record._id }}?_method=PUT" method="POST">
    <div class="mb-3">
      <label for="name" class="mb-2">名稱*</label>
      <input type="text" id="name" name="name" class="form-control" value="{{ record.name }}" required>
    </div>
    <div class="mb-3">
      <label for="date" class="mb-2">日期*</label>
      <input type="date" id="date" name="date" value="{{ record.date }}" min="2018-01-01" max="2060-12-31" required>
    </div>
    <div class="mb-3">
      <label for="categoryId">類別<span class="text-danger">*</span></label>
      <select name="categoryId" id="categoryId" class="form-select" aria-label="Default select example">
        {{#each categories}}
        <option value="{{this.id}}" {{#ifEquals this.id ../record.categoryId}} selected {{/ifEquals}}>
          {{this.name}}
        </option>
        {{/each}}
      </select>
    </div>
    <div class="mb-3">
      <label for="amount" class="mb-2">金額*</label>
      <input type="text" id="confirmPassword" name="amount" class="form-control" value="{{ record.amount }}" required>
    </div>
    <button type="submit" class="btn btn-primary btn-block">送出</button>
  </form>

</div>
</div>
</div>